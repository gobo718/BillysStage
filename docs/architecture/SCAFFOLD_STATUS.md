# Stage 3A Scaffold Status

The module directories in this repository are architectural boundaries, not active runtime locations yet.

- Runtime application files remain at their original v2.6.4 paths.
- No HTML script reference or production JavaScript path was changed.
- `MODULE.json` files are active governance files and are validated by tests.
- New cross-boundary code must follow the namespace conventions immediately.
- Existing mixed-boundary files remain listed in `BOUNDARY_EXCEPTIONS.md` until their assigned migration stages.
